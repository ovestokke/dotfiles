{{ if ne .chezmoi.os "windows" -}}
#!/bin/bash
# Sync agent and skill files between canonical source and tool-specific directories.
#
# Canonical agents:  ~/.agents/agents/*.md   (single source of truth; Pi reads these directly)
# OpenCode:          ~/.config/opencode/agents/*.md (OpenCode-specific frontmatter)
# Pi local agents:   ~/.pi/agent/agents/*.md (import-only; generated copies are removed)
# Skills:            ~/.agents/skills/
#
# Model policy:
#   - This sync does not add model pins.
#   - Pi reads canonical ~/.agents/agents directly, so existing canonical model:
#     fields are left untouched by this script.
#   - Generated OpenCode agents omit generic model and opencode_model by default,
#     so OpenCode uses its configured default model. Set AGENT_SYNC_KEEP_TOOL_MODELS=1
#     before running chezmoi if you want to preserve opencode_model pins.
#
# The script:
#   1. Imports new OpenCode/Pi-local agents into ~/.agents
#   2. Deploys canonical agents -> OpenCode (converting frontmatter)
#   3. Removes old script-generated Pi copies to avoid duplicate Pi agents
#   4. Mirrors changed canonical files into chezmoi source state
#   5. Optionally commits/pushes synced agent/skill source changes with git
# Only runs when monitored directories, source state, or model-policy env vars change.

set -uo pipefail

AGENTS_DIR="$HOME/.agents"
CANONICAL_AGENTS="$AGENTS_DIR/agents"
OPENCODE_AGENTS_DIR="$HOME/.config/opencode/agents"
PI_AGENTS_DIR="$HOME/.pi/agent/agents"
HASH_FILE="$HOME/.cache/chezmoi-agent-sync.hash"
SOURCE_ROOT="{{ .chezmoi.sourceDir }}"
SOURCE_AGENTS_DIR="$SOURCE_ROOT/dot_agents/agents"
SOURCE_SKILLS_DIR="$SOURCE_ROOT/dot_agents/skills"
AGENT_SYNC_KEEP_TOOL_MODELS="${AGENT_SYNC_KEEP_TOOL_MODELS:-0}"
AGENT_SYNC_GIT_COMMIT="${AGENT_SYNC_GIT_COMMIT:-1}"
AGENT_SYNC_GIT_PUSH="${AGENT_SYNC_GIT_PUSH:-1}"
SYNC_SCHEMA_VERSION="3"

mkdir -p "$CANONICAL_AGENTS" "$OPENCODE_AGENTS_DIR" "$PI_AGENTS_DIR" \
         "$AGENTS_DIR/skills" "$(dirname "$HASH_FILE")"

# --- Cross-platform file info (works on both GNU/Linux and macOS/BSD) ---
file_info() {
    local dir="$1" pattern="$2"
    {{- if eq .chezmoi.os "darwin" }}
    find "$dir" -name "$pattern" -exec stat -f '%N %m %z' {} \; 2>/dev/null
    {{- else }}
    find "$dir" -name "$pattern" -printf '%p %T@ %s\n' 2>/dev/null
    {{- end }}
}

file_info_recurse() {
    local dir="$1"
    {{- if eq .chezmoi.os "darwin" }}
    find "$dir" \( -type f -o -type l \) -exec stat -f '%N %m %z' {} \; 2>/dev/null
    {{- else }}
    find "$dir" \( -type f -o -type l \) -printf '%p %T@ %s\n' 2>/dev/null
    {{- end }}
}

stat_file() {
    local file="$1"
    {{- if eq .chezmoi.os "darwin" }}
    stat -f '%N %m %z' "$file" 2>/dev/null
    {{- else }}
    stat -c '%n %Y %s' "$file" 2>/dev/null
    {{- end }}
}

calc_hash() {
    {
        file_info "$CANONICAL_AGENTS" '*.md'
        file_info "$OPENCODE_AGENTS_DIR" '*.md'
        file_info "$PI_AGENTS_DIR" '*.md'
        file_info_recurse "$AGENTS_DIR/skills"
        [ -n "$SOURCE_ROOT" ] && file_info "$SOURCE_AGENTS_DIR" '*.md'
        [ -n "$SOURCE_ROOT" ] && file_info_recurse "$SOURCE_SKILLS_DIR"
        stat_file "$AGENTS_DIR/.skill-lock.json"
        printf 'SYNC_SCHEMA_VERSION=%s\n' "$SYNC_SCHEMA_VERSION"
        printf 'AGENT_SYNC_KEEP_TOOL_MODELS=%s\n' "$AGENT_SYNC_KEEP_TOOL_MODELS"
        printf 'AGENT_SYNC_GIT_COMMIT=%s\n' "$AGENT_SYNC_GIT_COMMIT"
        printf 'AGENT_SYNC_GIT_PUSH=%s\n' "$AGENT_SYNC_GIT_PUSH"
    } | sort | sha256sum | cut -d' ' -f1
}

# --- Hash check: skip if nothing changed ---
current_hash="$(calc_hash)"

previous_hash=""
[ -f "$HASH_FILE" ] && previous_hash="$(cat "$HASH_FILE")"

if [ "$current_hash" = "$previous_hash" ]; then
    exit 0
fi

echo "Syncing agent files..."

sync_failed=0
needs_literal_prefix() {
    case "$1" in
        dot_*|private_*|encrypted_*|executable_*|readonly_*|symlink_*|template_*|empty_*|literal_*|create_*|modify_*|remove_*|run_*|once_*|onchange_*|before_*|after_*)
            return 0
            ;;
        *)
            return 1
            ;;
    esac
}

source_basename_for_target() {
    local path="$1" base attr_prefix="" literal_prefix=""
    base="$(basename "$path")"

    if [ -L "$path" ]; then
        attr_prefix="symlink_"
    elif [ ! -s "$path" ]; then
        attr_prefix="empty_"
    elif [ -x "$path" ]; then
        attr_prefix="executable_"
    fi

    if needs_literal_prefix "$base"; then
        literal_prefix="literal_"
    fi

    printf '%s%s%s\n' "$attr_prefix" "$literal_prefix" "$base"
}

remove_source_variants() {
    local dir="$1" base="$2"
    rm -f \
        "$dir/$base" "$dir/literal_$base" \
        "$dir/executable_$base" "$dir/executable_literal_$base" \
        "$dir/symlink_$base" "$dir/symlink_literal_$base" \
        "$dir/empty_$base" "$dir/empty_literal_$base"
}

truthy() {
    case "$(printf '%s' "$1" | tr '[:upper:]' '[:lower:]')" in
        1|true|yes|on) return 0 ;;
        *) return 1 ;;
    esac
}

sync_file_to_source() {
    local path="$1" rel source_dir base source_base dest

    if [ -z "$SOURCE_ROOT" ]; then
        echo "chezmoi-agent-sync: cannot sync $path: chezmoi source path unavailable" >&2
        sync_failed=1
        return
    fi

    if [ "${path#"$CANONICAL_AGENTS/"}" != "$path" ]; then
        rel="${path#"$CANONICAL_AGENTS/"}"
        source_dir="$SOURCE_AGENTS_DIR/$(dirname "$rel")"
    elif [ "${path#"$AGENTS_DIR/skills/"}" != "$path" ]; then
        rel="${path#"$AGENTS_DIR/skills/"}"
        source_dir="$SOURCE_SKILLS_DIR/$(dirname "$rel")"
    else
        echo "chezmoi-agent-sync: refusing to sync non-canonical path $path" >&2
        sync_failed=1
        return
    fi

    base="$(basename "$rel")"
    source_base="$(source_basename_for_target "$path")"
    dest="$source_dir/$source_base"

    mkdir -p "$source_dir"
    remove_source_variants "$source_dir" "$base"

    if [ -L "$path" ]; then
        readlink "$path" > "$dest"
    else
        cp "$path" "$dest"
    fi
    chmod 0644 "$dest" 2>/dev/null || true
}

commit_synced_source_changes() {
    local git_root

    truthy "$AGENT_SYNC_GIT_COMMIT" || return 0
    git_root="$(git -C "$SOURCE_ROOT" rev-parse --show-toplevel 2>/dev/null || true)"
    [ -n "$git_root" ] || return 0

    git -C "$git_root" add -- "$SOURCE_AGENTS_DIR" "$SOURCE_SKILLS_DIR" || {
        echo "chezmoi-agent-sync: warning: failed to stage synced agent/skill source files" >&2
        return 0
    }

    git -C "$git_root" diff --cached --quiet -- "$SOURCE_AGENTS_DIR" "$SOURCE_SKILLS_DIR" && return 0

    if git -C "$git_root" commit -m "Update synced agents and skills" -- "$SOURCE_AGENTS_DIR" "$SOURCE_SKILLS_DIR"; then
        if truthy "$AGENT_SYNC_GIT_PUSH"; then
            git -C "$git_root" push || echo "chezmoi-agent-sync: warning: git push failed" >&2
        fi
    else
        echo "chezmoi-agent-sync: warning: git commit failed; synced files remain in source state" >&2
    fi
}

import_tool_agents() {
    local dir="$1" label="$2" agent name canonical
    for agent in "$dir"/*.md; do
        [ -f "$agent" ] || continue
        name="$(basename "$agent")"
        canonical="$CANONICAL_AGENTS/$name"
        [ -f "$canonical" ] && continue
        if [ "$label" = "Pi" ] && grep -q 'chezmoi-agent-sync' "$agent"; then
            continue
        fi
        echo "Importing $label agent $name -> ~/.agents/agents/"
        cp -a "$agent" "$canonical"
    done
}

# =============================================================================
# Helper: convert canonical agent to OpenCode format
# Strips fields that are not meaningful to OpenCode and renames opencode_* fields
# by removing the prefix. By default, opencode_model is omitted so OpenCode uses
# its configured default model. Set AGENT_SYNC_KEEP_TOOL_MODELS=1 to preserve
# opencode_model pins.
# =============================================================================
to_opencode() {
    awk -v keep_models="$AGENT_SYNC_KEEP_TOOL_MODELS" '
    function trim(s) { sub(/^[[:space:]]+/, "", s); sub(/[[:space:]]+$/, "", s); return s }
    function inherit_model(s, l) {
        l=tolower(trim(s))
        gsub(/^["\047]+|["\047]+$/, "", l)
        return (l == "" || l == "inherit" || l == "default" || l == "current" || l == "none" || l == "false")
    }
    function keep_tool_models() { return (keep_models == "1" || tolower(keep_models) == "true" || tolower(keep_models) == "yes") }
    BEGIN { in_front=0; front_done=0; skipping=0 }
    /^---$/ && !in_front && !front_done { in_front=1; print; next }
    /^---$/ && in_front { in_front=0; front_done=1; print; next }
    in_front {
        # Skip fields from other agent formats.
        if ($0 ~ /^name:/) { skipping=1; next }
        if ($0 ~ /^pi_/) { skipping=1; next }
        if ($0 ~ /^tools:/ && $0 !~ /^opencode_tools:/) { skipping=1; next }
        if ($0 ~ /^allowedTools:/) { skipping=1; next }
        if ($0 ~ /^model:/ && $0 !~ /^opencode_model:/) { skipping=1; next }
        # Continuation lines (indented) for skipped fields.
        if (skipping && /^  /) { next }
        skipping=0
        # Rename opencode_* fields -> strip prefix.
        if ($0 ~ /^opencode_model:/) {
            value=$0
            sub(/^opencode_model:[[:space:]]*/, "", value)
            if (keep_tool_models() && !inherit_model(value)) {
                sub(/^opencode_model:/, "model:")
                print
            }
            next
        }
        if ($0 ~ /^opencode_mode:/) { sub(/^opencode_mode:/, "mode:"); print; next }
        if ($0 ~ /^opencode_tools:/) { sub(/^opencode_tools:/, "tools:"); print; next }
        print
        next
    }
    { print }
    '
}

# =============================================================================
# Import: normal tool locations -> canonical ~/.agents
# =============================================================================
import_tool_agents "$OPENCODE_AGENTS_DIR" "OpenCode"
import_tool_agents "$PI_AGENTS_DIR" "Pi"

# =============================================================================
# Deploy: canonical -> OpenCode
# =============================================================================
for canonical in "$CANONICAL_AGENTS"/*.md; do
    [ -f "$canonical" ] || continue
    name="$(basename "$canonical")"
    to_opencode < "$canonical" > "$OPENCODE_AGENTS_DIR/$name"
done

# Clean up OpenCode agents that no longer exist in canonical.
for agent in "$OPENCODE_AGENTS_DIR"/*.md; do
    [ -f "$agent" ] || continue
    name="$(basename "$agent")"
    if [ ! -f "$CANONICAL_AGENTS/$name" ]; then
        rm -f "$agent"
    fi
done

# Pi also discovers ~/.agents/agents directly. Remove only old copies this script
# generated under ~/.pi/agent/agents to avoid duplicate Pi agents; leave manually
# created Pi-local agents alone.
for agent in "$PI_AGENTS_DIR"/*.md; do
    [ -f "$agent" ] || continue
    if grep -q 'chezmoi-agent-sync' "$agent"; then
        rm -f "$agent"
    fi
done

# =============================================================================
# Mirror canonical agents and skills into chezmoi source state
# =============================================================================
for agent in "$CANONICAL_AGENTS"/*.md; do
    [ -f "$agent" ] || continue
    sync_file_to_source "$agent"
done

# Add all skill files recursively (supports nested references/, scripts/, symlinks, etc.)
while IFS= read -r -d '' skill_file; do
    sync_file_to_source "$skill_file"
done < <(find "$AGENTS_DIR/skills" \( -type f -o -type l \) -print0 2>/dev/null)

if [ "$sync_failed" -ne 0 ]; then
    echo "chezmoi-agent-sync: not updating $HASH_FILE because one or more files failed to sync" >&2
    exit 1
fi

commit_synced_source_changes

# =============================================================================
# Update hash (recompute after deployments and successful source sync)
# =============================================================================
current_hash="$(calc_hash)"
echo "$current_hash" > "$HASH_FILE"
echo "Agent sync complete. Pi reads ~/.agents/agents directly; run /reload in Pi if needed."
{{- end }}
