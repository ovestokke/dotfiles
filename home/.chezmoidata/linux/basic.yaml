packages:
  linux:
    # Debian/Ubuntu packages (apt) - full desktop
    apt:
      - git # Version control system
      - eza # Modern ls replacement with icons
      - fzf # Fuzzy finder for terminal
      - zoxide # Smarter cd command (z)
      - curl # URL data transfer tool
      - wget # Network file downloader
      - unzip # ZIP archive extractor
      - ripgrep # Fast recursive grep alternative (rg)
      - fd-find # Fast find alternative (fdfind on Debian)
      - bat # Cat clone with syntax highlighting
      - gh # GitHub CLI
      - gnupg # GNU Privacy Guard encryption
      - fish # Friendly Interactive SHell
      - neovim # Vim-fork text editor
      - build-essential # GCC, make, and other build tools
      - wezterm # GPU-accelerated terminal emulator
      - nodejs # Node.js JavaScript runtime
      - npm # Node.js package manager
      # LazyVim dependencies
      - lazygit # Terminal UI for git
      - tree-sitter-cli # Parser generator for syntax highlighting
      - luajit # Just-In-Time Lua compiler

    # Debian/Ubuntu packages for WSL (minimal, no desktop apps)
    apt_wsl:
      - git # Version control system
      - eza # Modern ls replacement with icons
      - fzf # Fuzzy finder for terminal
      - zoxide # Smarter cd command (z)
      - curl # URL data transfer tool
      - wget # Network file downloader
      - unzip # ZIP archive extractor
      - ripgrep # Fast recursive grep alternative (rg)
      - fd-find # Fast find alternative (fdfind on Debian)
      - bat # Cat clone with syntax highlighting
      - gh # GitHub CLI
      - gnupg # GNU Privacy Guard encryption
      - fish # Friendly Interactive SHell
      - neovim # Vim-fork text editor
      - build-essential # GCC, make, and other build tools
      - nodejs # Node.js JavaScript runtime
      - npm # Node.js package manager
      # LazyVim dependencies
      - lazygit # Terminal UI for git
      - tree-sitter-cli # Parser generator for syntax highlighting
      - luajit # Just-In-Time Lua compiler

    # Arch Linux packages (pacman) - full desktop
    pacman:
      - git # Version control system
      - eza # Modern ls replacement with icons
      - fzf # Fuzzy finder for terminal
      - zoxide # Smarter cd command (z)
      - curl # URL data transfer tool
      - wget # Network file downloader
      - unzip # ZIP archive extractor
      - ripgrep # Fast recursive grep alternative (rg)
      - fd # Fast find alternative
      - bat # Cat clone with syntax highlighting
      - github-cli # GitHub CLI (gh)
      - gnupg # GNU Privacy Guard encryption
      - fish # Friendly Interactive SHell
      - neovim # Vim-fork text editor
      - base-devel # GCC, make, and other build tools
      - wezterm # GPU-accelerated terminal emulator
      - ttf-meslo-nerd # Nerd Font patched Meslo font
      - nodejs # Node.js JavaScript runtime
      - npm # Node.js package manager
      # LazyVim dependencies
      - lazygit # Terminal UI for git
      - tree-sitter-cli # Parser generator for syntax highlighting
      - luajit # Just-In-Time Lua compiler

    # Arch Linux packages for WSL (minimal, no desktop apps)
    pacman_wsl:
      - git # Version control system
      - eza # Modern ls replacement with icons
      - fzf # Fuzzy finder for terminal
      - zoxide # Smarter cd command (z)
      - curl # URL data transfer tool
      - wget # Network file downloader
      - unzip # ZIP archive extractor
      - ripgrep # Fast recursive grep alternative (rg)
      - fd # Fast find alternative
      - bat # Cat clone with syntax highlighting
      - github-cli # GitHub CLI (gh)
      - gnupg # GNU Privacy Guard encryption
      - fish # Friendly Interactive SHell
      - neovim # Vim-fork text editor
      - base-devel # GCC, make, and other build tools
      - nodejs # Node.js JavaScript runtime
      - npm # Node.js package manager
      # LazyVim dependencies
      - lazygit # Terminal UI for git
      - tree-sitter-cli # Parser generator for syntax highlighting
      - luajit # Just-In-Time Lua compiler

    # AUR packages (yay/paru) - packages not in official repos
    aur:
      - ghostty # Fast GPU-accelerated terminal emulator
      - proton-pass-bin # Proton Pass CLI for password management (pre-built binary)
